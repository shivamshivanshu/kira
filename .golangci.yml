version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - revive
  settings:
    revive:
      enable-default-rules: true
      rules:
        - name: exported
          disabled: true
        - name: package-comments
          disabled: true
